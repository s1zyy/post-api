CREATE TABLE codes
(
    id          BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    code        VARCHAR(255),
    user_id     BIGINT,
    expiry_date TIMESTAMP WITHOUT TIME ZONE,
    CONSTRAINT pk_codes PRIMARY KEY (id)
);

ALTER TABLE codes
    ADD CONSTRAINT FK_CODES_ON_USER FOREIGN KEY (user_id) REFERENCES users (id);